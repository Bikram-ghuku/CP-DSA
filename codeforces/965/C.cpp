#include<bits/stdc++.h>
using namespace std;

int calcScore(vector<int> ds){
    int n = ds.size();
    for(int i = 0; i < n; i++){

    }
}

void solve(){
    int n, k;
    cin>>n>>k;
    vector<int> a(n), b(n);

    for(int i = 0; i < n; i++) cin>>a[i];
    for(int i = 0; i < n; i++) cin>>b[i];
}

int main(){
    int t;
    cin>>t;
    while(t--){
        solve();
    }
}